# v0.4.0 Monthly Archive Acceptance Evidence — 2026-08-05

This record covers the schema-v2 ICON-REA-L-CH1 backfill layout introduced to
avoid excessive inode use and the final v0.4.0 accumulation-difference
correction. The schema-v2 layout itself changes only campaign orchestration and
publication. The final candidate also replaces the nonphysical negative-delta
reset substitution with a uniform clamp-and-count policy for realtime and REA;
the daily `0000` boundary and 24 hourly REA outputs remain unchanged.

## Automated and packaging gates

- Python compilation, Ruff, and mypy: pass
- pytest: `139 passed`, total coverage `81.30%`
- benchmark: Numba backend, approximately 415,392 active columns/s
- `pip check`: no broken requirements
- wheel: `precip_type_diag-0.4.0-py3-none-any.whl`
- wheel inspection: backfill and GRIB archive modules, packaged ecCodes
  overlays, and all three console entry points present

## `/store_new` evidence

The inspected target `/store_new/mch` is Lustre. At validation time it reported:

- capacity: 4.4 PB total, 4.2 PB used, 236 TB available, 95% used;
- inodes: 150,000,000 total, 135,464,202 used, 14,535,798 available, 91% used.

The planned `20050101..20250831` range contains 7,548 daily cycles and 181,152
hourly messages. Schema v2 stores those messages in 248 monthly GRIB2 files plus
one archive contract: 249 files on `/store_new`, rather than 181,153. Campaign
receipts, locks, logs, manifest, script, and status remain on `/users`; the
planner projects 747 campaign-root files and 996 persistent files overall.

At 55,107,816 bytes per representative day, categorical output projects to
415,953,795,168 bytes (416 GB, 387 GiB, 0.38 TiB). The initial Firdewsa January
run averaged 135.8 seconds of daily science wall time, so eight concurrent
monthly tasks imply about 36 hours of ideal processing time. The final
ICON-mode measurement and conservative operational estimate are recorded
below.

## Live Balfrin/FDB evidence

| Job | Partition | Exercise | Result |
| ---: | --- | --- | --- |
| `5013319` | `pp-short` | process two REA days, publish one monthly archive, verify, then rerun | completed in 6m15s, exit `0:0` |
| `5013347` | `pp-short` | independent ecCodes `grib_count`/`grib_ls` inspection | 48 of 48 messages readable |
| `5013370` | `pp-short` | exact updated source, one day, bounded staging, receipt preservation, verify, rerun | completed in 3m30s, exit `0:0` |
| `5013374` | `pp-short` | exact final verifier including receipt SHA-256 recomputation | complete, exit `0:0` |
| `5013423` | `pp-short` | inventory-plan all January 2010 cycles and required fields | 31 of 31 cycles selected, zero retries |
| `5013424` | `pp-long` | process all 31 cycles and atomically publish one monthly archive | completed in 1h10m57s, exit `0:0` |
| `5013766`, `5013768` | `pp-short` | full metadata/checksum verification, then no-FDB restart | complete; restart returned `verified_existing` |
| `5013770`, `5013794`, `5013818` | `pp-short` | independent ecCodes message, metadata, extrema, and category scan | 744 valid PTYPE messages, no invalid codes |

The two-day archive is 110,215,632 bytes and has SHA-256
`a154736f1090fdfd6c23feb6b434941b2fbf26f0401bc6091de4b2297aaa9152`.
ecCodes read all 48 concatenated messages in file order. Message 1 is cycle
`20100101`, step 1, valid at `20100101 0100`; message 48 is cycle `20100102`,
step 24, valid at `20100103 0000`. Every inspected message used
`shortName=PTYPE` and `paramId=502712`.

The exact-source one-day archive is 55,107,816 bytes and has SHA-256
`d4656331879f71ddc6c3b3a41e391cee08eeebae91d4e3ffbf2d69ca0ba80c10`.
Its receipt preserves the Firdewsa fidelity record, provenance, data-quality
counts, retry statistics, runtime, first/last metadata, message count, size,
and checksum. It recorded zero FDB retries. The second invocation returned
`verified_existing`; `backfill-status --verify-outputs` passed after reading all
messages and recomputing the checksum.

During processing, only a hidden `.partial` file was visible in the archive
root. The final name appeared only after stream validation and atomic rename.
After completion there were exactly two archive-root files (the monthly GRIB2
file and `ARCHIVE_CONTRACT.json`), no hidden partial, and no daily staging files.

The full January 2010 archive was produced from clean commit
`f7ec6477f2e1548781a295c58dc821b97445efa7` with package version `0.4.0`. It
contains 744 PTYPE-only messages and occupies 1,708,342,296 bytes. Its SHA-256 is
`10141671390dd11a0307b26e1ab996610609bf45b3d8808449ab845755da6eb0`.
Message 24 is cycle `20100101` step 24, valid at `20100102 0000`; message 25
starts cycle `20100102` at step 1; message 744 is cycle `20100131` step 24,
valid at `20100201 0000`.

All 31 daily data-quality summaries reported zero invalid columns, zero
exhausted FDB operations, and zero FDB retries. Daily wall times ranged from
112.2 to 176.3 seconds, with a 131.5-second median and 135.8-second mean. An
independent value scan found only category codes `0,1,3,5,8,12,13`, all within
the accepted contract. The completed scratch root was empty, full verification
recomputed the receipt checksum successfully, and rerunning the monthly task
did not retrieve or regenerate data.

## Accumulation-difference correction

The January ICON comparison exposed a flaw in the previous negative-delta rule.
It found 3,559,836 negative `TOT_PREC` adjacent differences, none below
`-0.002 mm`. Treating each as a reset and substituting the full current
accumulator activated 1,904,701 otherwise inactive ICON columns. The same rule
also made 266,261 raw-active columns report an archived phase-component sum
above 150% of total precipitation; simply clamping negative component
differences to zero reduced that count to 16.

Live realtime audit job `5015583` (`pp-short`, completed in 27 seconds, exit
`0:0`) confirmed that this is not REA-specific. Between steps 1 and 2 of the
`20260805 1200` CH1 cycle, control/member 010 had 4,656/6,802 negative
`TOT_PREC` differences out of 1,147,980 grid points. Every decrease was exactly
or less negative than `-0.0009765625 mm`, yet the old reset substitution would
have assigned current accumulations as large as 35.408/33.514 mm to those
hours. The `20260805 0600` CH2 control had 621 negative differences out of
283,876, with a minimum of `-0.000030517578125 mm`; member 020 had none. No
sampled CH1 or CH2 decrease was below `-0.002 mm`, and the component fields
showed the same packing-scale behavior. The audit made 40 FDB retrieval/decode
attempts with zero retries or exhausted operations.

The corrected contract therefore applies to realtime and REA alike: an
accumulator must be monotonic inside its cycle, every negative decoded adjacent
difference is clamped to zero, and clamp counts remain visible in
`summary.json`. A decrease is never converted into the full current
accumulation.

## Exact-candidate realtime acceptance

Clean detached commit `10a46b1f7c1196f07cb7cbcd0733e2a7a2b2a318` was
installed through `tools/setup_balfrin.sh` with `fdb/5.21:v1`. GitHub Actions
passed the same 139-test gate. The production-facing realtime results were:

| Job | Exercise | Result |
| ---: | --- | --- |
| `5015635` | CH1 all 11 members, ICON, steps 1–2, NetCDF and probabilities | completed in 1m27s, monitoring `ok` |
| `5015636` | CH2 all 21 members, ICON, steps 1–2, NetCDF and probabilities | completed in 1m23s, monitoring `ok` |
| `5015656` | pinned executable ICON Fortran comparison | 13 of 13 cases matched commit `50da7c5` |
| `5015662` | independent scan of every realtime member/probability file | completed in 20s, exit `0:0` |

CH1 wrote 22 member files and two probability files. It reported 47,778
clamped negative `TOT_PREC` differences and 255,223 clamped archived-component
differences across its two hours, with no invalid input columns or failed
members. CH2 wrote 42 member files and two probability files and reported
8,912/7,378 corresponding clamps, again with no invalid inputs or failures.
Both probability aggregations used every requested member and completed with
status `ok`.

The independent scan read all 68 member NetCDF files and four probability
files. All hourly precipitation values were finite and nonnegative; observed
ranges were `0..75.3818359375 mm` for CH1 and `0..13.78662109375 mm` for CH2.
All PTYPE values were in the allowed ICON category set, all valid-member counts
were exactly 11/21, and every probability was within `0..100%`.

## Corrected January ICON acceptance

| Job | Exercise | Result |
| ---: | --- | --- |
| `5015591` | plan all January 2010 ICON cycles and required fields | 31 of 31 selected, zero retries |
| `5015646_0` | process and atomically publish the corrected ICON month | completed in 1h19m54s, exit `0:0` |
| `5016907` | full metadata, ordering, message, and receipt-checksum verification | complete, 31 of 31 cycles verified |
| `5016908` | exact old-policy versus corrected-policy archive comparison | 744 aligned messages, exit `0:0` |
| `5016909` | independent corrected ICON/Firdewsa, physical-rule, component, and `TOT_PR` scan | completed in 11m53s, exit `0:0` |

The corrected archive contains exactly 744 PTYPE-only messages and occupies
1,708,342,296 bytes. Its SHA-256 is
`24bbd1923690098c2b24335cf6ab94632bb5718f4e696448bece7f8435bb4191`.
The first record is cycle `20100101`, step 1, valid at 01 UTC; the last is
cycle `20100131`, step 24, valid at `20100201 00 UTC`. The completed scratch
root is empty. All 31 daily summaries report zero invalid active profile,
ground-temperature, or precipitation columns and zero FDB retries. They record
3,559,836 clamped negative total-precipitation differences and 7,565,108
clamped archived-component differences, making the packing condition fully
observable.

The old and corrected archives have identical message metadata, shape, and file
size. The correction changes 2,096,913 of 854,097,120 cell-hours (`0.246%`). Of
these, 1,904,701 are old nonzero classifications becoming `NO_PRECIP`, exactly
the population falsely activated by the reset substitution. The remaining
changes are phase refinements from corrected rain/snow/graupel component rates.
Notable removals are 849,268 rain, 618,368 snow, 402,992 freezing-drizzle, and
18,137 graupel false activations. No corrected nonzero output is below the
implemented precipitation mask.

At the intended grain of one grid cell per valid hour, completeness and domain
validity pass: both compared archives contain 744 unique ordered date/step
records, 1,147,980 cells per record, no invalid category, and no missing
required field. The independent scan found zero violations of all encoded ICON
rules for rain/freezing-rain wet-bulb sign, freezing-rain-on-ground ground
temperature, wet-snow temperature range, graupel dominance, rain/snow mixing,
and unavailable hail. The corrected archived phase sum reconstructs total
precipitation closely: mean ratio `0.999794`; only 16 of 204,279,796 active
cells exceed 1.5 and only six fall below 0.5. These checks support physical
credibility, while exact online refinement parity remains unavailable because
convective rain/snow and hail rates are not archived.

Corrected ICON diagnoses 204,279,796 active cell-hours (`23.92%`) versus
307,461,338 (`36.00%`) for Firdewsa. Exact categorical agreement is `84.16%`;
103,207,603 Firdewsa nonzero cells remain ICON `NO_PRECIP`, while only 26,061
Firdewsa-zero cells are ICON nonzero after removing the reset artifact. The
corrected category distribution is physically coherent for January: rain has
median surface wet bulb `4.375 C`, snow `-4.375 C`, freezing rain `-0.875 C`,
ice pellets `-2.125 C`, and freezing drizzle `-3.625 C`. Hail is absent, as
expected without archived hail rate.

`TOT_PR` remains a timing diagnostic, not an ICON input. The hourly amount mask
and instantaneous-rate mask overlap in 181,383,359 cells and have Jaccard
agreement `84.47%`. Of corrected ICON nonzero cells, 22,896,437 (`11.21%`) have
instantaneous `TOT_PR <= 0.01 mm/h`; conversely 10,442,578 instantaneous-active
cells have an hourly amount below threshold. This symmetric interval-versus-
instant mismatch explains those cases without implying an internal PTYPE
completeness failure: every corrected hourly-active cell receives a nonzero
category, and every hourly-inactive cell receives zero.

Daily science wall times range from 116.0 to 192.3 seconds, with median 151.3
and mean 152.4 seconds. Scaling the measured mean to 7,548 cycles gives about
320 serial compute-hours or 40 ideal wall-hours with eight concurrent monthly
tasks. The unchanged packing projects to 415,953,795,168 bytes (`416 GB`,
`387 GiB`, `0.38 TiB`), 248 monthly archive files, 249 files in the archive root,
and about 996 persistent files including campaign control state. Operations
should reserve at least `0.5 TiB` plus retention margin and budget two to three
days including queue and retry margin.

## Operational conclusion

Plain GRIB concatenation is valid and interoperable for this product. The
implementation performs the equivalent byte concatenation only after each day
passes the existing science and output checks, assigns one writer to a month,
validates ordering and D+1 semantics, calls `fsync`, and publishes atomically.
The schema-v2 layout is suitable for subset or full REA processing without
creating an unsafe number of files on `/store_new`.

Before launching all 20 years, operations still needs to confirm the current
long-term destination and retention owner. Schema-v1 manifests remain bound to
release `v0.3.0` and must be re-planned with v0.4.0.
