# v0.3.0 Candidate Acceptance Evidence — 2026-08-04

This record covers the Balfrin/FDB orchestration candidate implementing
progressive EPS publication and cycle-aware REA-L-CH1 backfills. It records
technical evidence; it does not replace named scientific and operational
approval required before an accepted tag.

## Candidate and runtime

- package version: `0.3.0`
- candidate workspace: pre-commit repository state subsequently committed as
  the revision referenced by this record
- Balfrin candidate path:
  `/users/olifu/work/precip_type_diag-candidate-20260804-codex`
- acceptance output/log root:
  `/users/olifu/work/ptype-acceptance-20260804`
- `uenv`: `8.1.0`
- image: `fdb/5.21:v1` (`5568c10c66ad5870`, 2026-07-15)
- views: `realtime`, `rea-l-ch1`
- setup: `tools/setup_balfrin.sh`; dependency and import checks passed
- partitions: `pp-short` for bounded smokes, `pp-long` for the daily restart

## Automated and packaging gates

The complete repository gate passed:

- Python compilation: pass
- Ruff: pass
- mypy: pass
- pytest: `131 passed`, total coverage `80.88%`
- benchmark: Numba backend, 15,000 active / 20,000 total columns,
  approximately 409,669 columns/s
- `pip check`: no broken requirements
- `git diff --check`: pass

The `0.3.0` wheel built without packaging deprecation warnings. Inspection
confirmed `backfill.py`, `realtime.py`, all ecCodes definition overlays, and the
three console entry points (`precip-type-diag`, `-realtime`, and `-backfill`).

## Scheduled Balfrin jobs

| Job | Partition | Path exercised | Result |
| ---: | --- | --- | --- |
| `5010258` | `pp-short` | CH2 Firdewsa progressive step 1 then step 2 | completed, exit `0:0` |
| `5010260` | `pp-short` | REA Firdewsa step 1 for 2005, 2015, and 2025 | completed, exit `0:0` |
| `5010269` | `pp-long` | retained partial REA day to manifest task 1..24, then verified status | completed, exit `0:0` |
| `5010270` | `pp-short` | CH1 short-cycle Firdewsa progressive step 1 then step 2 | completed, exit `0:0` |
| `5010271` | `pp-short` | CH1 all-member ICON mode, step 1 | completed, exit `0:0` |
| `5010272` | `pp-short` | CH2 all-member ICON mode, step 1 | completed, exit `0:0` |
| `5010273` | `pp-short` | REA ICON mode, step 1 | completed, exit `0:0` |
| `5010349` | `pp-short` | CH1 long-cycle horizon discovery and all-member step 1 | completed, exit `0:0` |
| `5010493` | `pp-short` | metadata-aware REA 24-file skip verification and campaign status | completed, exit `0:0` |

## Realtime evidence

CH1 cycle `20260804 1500` recorded horizon 33; CH1 cycle `20260804 0300`
recorded horizon 45 and discovered all 45 contiguous hours. CH2 cycle
`20260804 1200` recorded horizon 120.

The short CH1 and CH2 runs each contain two successful increments `(1,1)` and
`(2,2)`. Step-1 probability files remained present after step 2. CH1 has 11
member directories and CH2 has 21; both have two probability files and no
critical alerts.

Inspected step-2 products:

| Product | Shape | Codes / range | Contract |
| --- | ---: | --- | --- |
| CH1 member `000` | `1,147,980` | `0,1,5,12` | model/cycle/member/step/algorithm attributes correct |
| CH1 probability | 21 variables | probability fields `0..100`; valid count exactly 11 | `percent_0_100` |
| CH2 member `000` | `283,876` | `0,1,5` | model/cycle/member/step/algorithm attributes correct |
| CH2 probability | 21 variables | probability fields `0..100`; valid count exactly 21 | `percent_0_100` |

ICON-mode summaries report `icon_adapted_partial_archive`, successful
monitoring, and all expected members. They correctly keep
`exact_online_refinement_parity=false` because convective rain/snow and hail
components are unavailable in the selected archive.

## REA-L-CH1 evidence

The interrupted/restarted `20100101 0000` cycle finished with 24 readable GRIB2
files, `DONE.json`, successful monitoring, a complete receipt, and
`campaign-status.json` with output verification enabled.
Re-running its array task with the final metadata-aware verifier returned
`verified_existing` without FDB reprocessing.

Step 24 inspection showed:

- `shortName=PTYPE`, `paramId=502712`;
- source date/time `20100101 0000`, step/endStep `24`;
- valid date/time `20100102 0000`;
- shape `1,147,980` and codes `0,1,3,5,12,13`.

Representative step-1 outputs for `20050101`, `20150101`, and `20250831` all
passed monitoring and GRIB decoding. Their validity times are cycle date
`01 UTC`, while the full-day step 24 remains correctly tied to the preceding
cycle date and valid at the following midnight.

## Defects found by live acceptance

Live validation found and verified fixes for two operational issues:

1. The venv Python resolves inside the uenv mount and is a dangling symlink
   outside it. The unified runner now accepts that expected symlink; a static
   regression test covers the readiness check.
2. A one-day REA plan initially queried the whole archive for every required
   field. Inventory queries now use the requested FDB date range. The retained
   partial day was then completed by the optimized manifest task.

FDB inspection also confirmed the cycle-specific CH1 contract: most cycles end
at 33, while the reviewed `0300` cycle extends all members to 45. Unit and live
tests cover both horizons.

## Approval state

- technical implementation evidence: complete
- scientific behavior change: none; Firdewsa remains the accepted default
- scientific approver: pending
- operational approver: pending
- accepted tag: pending both approvals
